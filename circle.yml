## Customize test commands
  test:
    override:
      - case $CIRCLE_NODE_INDEX in 0) ./gradlew phantomJsTest --stacktrace --debug ;; 1) ./gradlew phantomJsTest --stacktrace --debug ;; esac:
        paralle: true